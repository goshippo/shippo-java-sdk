/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.goshippo.shippo_sdk;

import com.goshippo.shippo_sdk.utils.HTTPClient;
import com.goshippo.shippo_sdk.utils.Headers;
import com.goshippo.shippo_sdk.utils.Hook.SdkInitData;
import com.goshippo.shippo_sdk.utils.RetryConfig;
import com.goshippo.shippo_sdk.utils.SpeakeasyHTTPClient;
import com.goshippo.shippo_sdk.utils.Utils;
import java.lang.String;
import java.util.Map;
import java.util.Optional;
import java.util.function.Consumer;

/**
 * Shippo external API.: Use this API to integrate with the Shippo service
 */
public class Shippo {
    private static final Headers _headers = Headers.EMPTY;


    /**
     * SERVERS contains the list of server urls available to the SDK.
     */
    public static final String[] SERVERS = {

        "https://api.goshippo.com",
    };

    /**
     * Addresses are the locations a parcel is being shipped **from** and **to**. They represent company
     * and residential places. Among other things, you can use address objects to create shipments,
     * calculate shipping rates, and purchase shipping labels.
     */
    private final Addresses addresses;

    /**
     * A batch is a technique for creating multiple labels at once. Use the  batch object to create and
     * purchase many shipments in two API calls. After creating the batch, retrieve the batch to verify
     * that all shipments are valid.
     * 
     * <p>You can add and remove shipments after you have created the batch. When all shipments are valid you
     * can purchase the batch and retrieve all the shipping labels.
     */
    private final Batches batches;

    /**
     * Carriers are the companies who deliver your package. Shippo uses Carrier account objects as
     * credentials to retrieve shipping rates and purchase labels from shipping Carriers.
     */
    private final CarrierAccounts carrierAccounts;

    /**
     * Customs declarations are relevant information, including one or multiple customs items, you need to
     * provide for
     * customs clearance for your international shipments.
     */
    private final CustomsDeclarations customsDeclarations;

    /**
     * Customs declarations are relevant information, including one or multiple customs items, you need to
     * provide for customs clearance for your international shipments.
     */
    private final CustomsItems customsItems;

    /**
     * Rates at checkout is a tool for merchants to display up-to-date shipping estimates based on what's
     * in their customers cart and where they’re shipping to.
     * Merchants set up curated shipping options for customers in the checkout flow based on data in the
     * shopping cart. The request must include the **to** address and item information. Optional fields are
     * the **from** address and package information.
     * 
     * <p>If the optional fields are not included, the service will use the default address and/or package
     * configured for rates at checkout. The response is a list of shipping options based on the Service
     * Group configuration.
     * (see [Service Group configuration](/shippoapi/public-api/service-groups) for details).
     * 
     * <p># Default Parcel Template
     * Assign one of your user parcel templates to be the default used when generating Live Rates. This
     * template will be used by default when generating Live Rates, unless you explicitly provide a parcel
     * in the Live Rates request.
     */
    private final RatesAtCheckout ratesAtCheckout;

    /**
     * A manifest is a single-page document with a barcode that carriers can scan to accept all packages
     * into transit without the need to scan each item individually.
     * They are close-outs of shipping labels of a certain day. Some carriers require manifests to  process
     * the shipments.
     */
    private final Manifests manifests;

    /**
     * An order is a request from a customer to purchase goods from a merchant.
     * Use the orders object to load orders from your system to the Shippo dashboard.
     * You can use the orders object to create, retrieve, list, and manage orders programmatically.
     * You can also retrieve shipping rates, purchase labels, and track shipments for each order.
     */
    private final Orders orders;

    /**
     * A carrier parcel template represents a package used for shipping that has preset dimensions defined
     * by a carrier. Some examples of a carrier parcel template include USPS Flat Rate Box and Fedex Small
     * Pak. When using a carrier parcel template, the rates returned may be limited to the carrier that
     * provides the box.
     * 
     * <p>You can create user parcel templates using a carrier parcel template. Shippo takes the dimensions of
     * the carrier parcel template but you must configure the weight.
     */
    private final CarrierParcelTemplates carrierParcelTemplates;

    /**
     * A parcel is an item you are shipping. The parcel object includes details about its physical make-up
     * of the parcel. It includes dimensions and weight that Shippo uses to calculate rates.
     */
    private final Parcels parcels;

    /**
     * A pickup is when you schedule a carrier to collect a package for delivery.
     * Use Shippo’s pickups endpoint to schedule pickups with USPS and DHL Express for eligible shipments
     * that you have already created.
     */
    private final Pickups pickups;

    /**
     * A rate is the cost to ship a parcel from a carrier. The rate object details the service level
     * including the cost and transit time.
     */
    private final Rates rates;

    /**
     * Refunds are reimbursements for successfully created but unused shipping labels or other charges.
     */
    private final Refunds refunds;

    /**
     * A service group is a set of service levels grouped together.
     * Rates at checkout uses services groups to present available shipping options to customers in their
     * shopping basket.
     */
    private final ServiceGroups serviceGroups;

    /**
     * A shipment is the act of transporting goods. A shipment object contains **to** and **from**
     * addresses, and the parcel details that you are shipping. You can use the shipment object to retrieve
     * shipping rates and purchase a shipping label.
     */
    private final Shipments shipments;

    /**
     * <p style="text-align: center; background-color: #F2F3F4;"><br>
     * If you purchased your shipping label through Shippo, you can also get all the tracking details of
     * your Shipment
     * from the [Transaction](/shippoapi/public-api/transactions) object.
     * <br><br>&lt;/p&gt;
     * A tracking status of a package is an indication of current location of a package in the supply
     * chain. For example,  sorting, warehousing, or out for delivery. Use the tracking status object to
     * track the location of your shipments.
     * 
     * <p>When using your [Test](https://docs.goshippo.com/docs/guides_general/authentication/) token for
     * tracking, you need to use Shippo's
     * predefined tokens for testing different tracking statuses. You can find more information in our
     * [Tracking tutorial](https://docs.goshippo.com/docs/tracking/tracking/) on how to do this, and what
     * the
     * payloads look like.
     */
    private final TrackingStatus trackingStatus;

    /**
     * A transaction is the purchase of a shipping label from a shipping provider for a specific service.
     * You can print purchased labels and used them to ship a parcel with a carrier, such as USPS or FedEx.
     */
    private final Transactions transactions;

    /**
     * A user parcel template represents a package used for shipping that has preset dimensions and
     * attributes defined
     * by you. They are useful for capturing attributes of parcel-types you frequently use for shipping,
     * allowing
     * them to be defined once and then used for many shipments. These parcel templates can also be used
     * for live rates.
     * 
     * <p>User parcel templates can also be created using a carrier parcel template, where the dimensions will
     * be copied from
     * the carrier presets, but the weight can be configured by you.
     */
    private final UserParcelTemplates userParcelTemplates;

    /**
     * Shippo Accounts are used by Shippo Platform Accounts to create and manage Managed Shippo Accounts.
     * Managed Shippo Accounts are headless accounts that represent your customers. They are opaque to your
     * end customers, meaning customers do not need to create their own Shippo login or have a billing
     * relationship with Shippo.
     * They can be used by marketplaces, e-commerce platforms, and third-party logistics providers who want
     * to offer, seamless, built-in shipping functionality to their customers. See our
     * [guide](https://docs.goshippo.com/docs/platformaccounts/platform_accounts/) for more details.
     */
    private final ShippoAccounts shippoAccounts;

    /**
     * Webhooks are a way for Shippo to notify your application when a specific event occurs. For example,
     * when a label is purchased or when a shipment tracking status has changed. You can use webhooks to
     * trigger actions in your application, such as sending an email or updating a database.
     */
    private final Webhooks webhooks;

    /**
     * Addresses are the locations a parcel is being shipped **from** and **to**. They represent company
     * and residential places. Among other things, you can use address objects to create shipments,
     * calculate shipping rates, and purchase shipping labels.
     */
    public Addresses addresses() {
        return addresses;
    }

    /**
     * A batch is a technique for creating multiple labels at once. Use the  batch object to create and
     * purchase many shipments in two API calls. After creating the batch, retrieve the batch to verify
     * that all shipments are valid.
     * 
     * <p>You can add and remove shipments after you have created the batch. When all shipments are valid you
     * can purchase the batch and retrieve all the shipping labels.
     */
    public Batches batches() {
        return batches;
    }

    /**
     * Carriers are the companies who deliver your package. Shippo uses Carrier account objects as
     * credentials to retrieve shipping rates and purchase labels from shipping Carriers.
     */
    public CarrierAccounts carrierAccounts() {
        return carrierAccounts;
    }

    /**
     * Customs declarations are relevant information, including one or multiple customs items, you need to
     * provide for
     * customs clearance for your international shipments.
     */
    public CustomsDeclarations customsDeclarations() {
        return customsDeclarations;
    }

    /**
     * Customs declarations are relevant information, including one or multiple customs items, you need to
     * provide for customs clearance for your international shipments.
     */
    public CustomsItems customsItems() {
        return customsItems;
    }

    /**
     * Rates at checkout is a tool for merchants to display up-to-date shipping estimates based on what's
     * in their customers cart and where they’re shipping to.
     * Merchants set up curated shipping options for customers in the checkout flow based on data in the
     * shopping cart. The request must include the **to** address and item information. Optional fields are
     * the **from** address and package information.
     * 
     * <p>If the optional fields are not included, the service will use the default address and/or package
     * configured for rates at checkout. The response is a list of shipping options based on the Service
     * Group configuration.
     * (see [Service Group configuration](/shippoapi/public-api/service-groups) for details).
     * 
     * <p># Default Parcel Template
     * Assign one of your user parcel templates to be the default used when generating Live Rates. This
     * template will be used by default when generating Live Rates, unless you explicitly provide a parcel
     * in the Live Rates request.
     */
    public RatesAtCheckout ratesAtCheckout() {
        return ratesAtCheckout;
    }

    /**
     * A manifest is a single-page document with a barcode that carriers can scan to accept all packages
     * into transit without the need to scan each item individually.
     * They are close-outs of shipping labels of a certain day. Some carriers require manifests to  process
     * the shipments.
     */
    public Manifests manifests() {
        return manifests;
    }

    /**
     * An order is a request from a customer to purchase goods from a merchant.
     * Use the orders object to load orders from your system to the Shippo dashboard.
     * You can use the orders object to create, retrieve, list, and manage orders programmatically.
     * You can also retrieve shipping rates, purchase labels, and track shipments for each order.
     */
    public Orders orders() {
        return orders;
    }

    /**
     * A carrier parcel template represents a package used for shipping that has preset dimensions defined
     * by a carrier. Some examples of a carrier parcel template include USPS Flat Rate Box and Fedex Small
     * Pak. When using a carrier parcel template, the rates returned may be limited to the carrier that
     * provides the box.
     * 
     * <p>You can create user parcel templates using a carrier parcel template. Shippo takes the dimensions of
     * the carrier parcel template but you must configure the weight.
     */
    public CarrierParcelTemplates carrierParcelTemplates() {
        return carrierParcelTemplates;
    }

    /**
     * A parcel is an item you are shipping. The parcel object includes details about its physical make-up
     * of the parcel. It includes dimensions and weight that Shippo uses to calculate rates.
     */
    public Parcels parcels() {
        return parcels;
    }

    /**
     * A pickup is when you schedule a carrier to collect a package for delivery.
     * Use Shippo’s pickups endpoint to schedule pickups with USPS and DHL Express for eligible shipments
     * that you have already created.
     */
    public Pickups pickups() {
        return pickups;
    }

    /**
     * A rate is the cost to ship a parcel from a carrier. The rate object details the service level
     * including the cost and transit time.
     */
    public Rates rates() {
        return rates;
    }

    /**
     * Refunds are reimbursements for successfully created but unused shipping labels or other charges.
     */
    public Refunds refunds() {
        return refunds;
    }

    /**
     * A service group is a set of service levels grouped together.
     * Rates at checkout uses services groups to present available shipping options to customers in their
     * shopping basket.
     */
    public ServiceGroups serviceGroups() {
        return serviceGroups;
    }

    /**
     * A shipment is the act of transporting goods. A shipment object contains **to** and **from**
     * addresses, and the parcel details that you are shipping. You can use the shipment object to retrieve
     * shipping rates and purchase a shipping label.
     */
    public Shipments shipments() {
        return shipments;
    }

    /**
     * <p style="text-align: center; background-color: #F2F3F4;"><br>
     * If you purchased your shipping label through Shippo, you can also get all the tracking details of
     * your Shipment
     * from the [Transaction](/shippoapi/public-api/transactions) object.
     * <br><br>&lt;/p&gt;
     * A tracking status of a package is an indication of current location of a package in the supply
     * chain. For example,  sorting, warehousing, or out for delivery. Use the tracking status object to
     * track the location of your shipments.
     * 
     * <p>When using your [Test](https://docs.goshippo.com/docs/guides_general/authentication/) token for
     * tracking, you need to use Shippo's
     * predefined tokens for testing different tracking statuses. You can find more information in our
     * [Tracking tutorial](https://docs.goshippo.com/docs/tracking/tracking/) on how to do this, and what
     * the
     * payloads look like.
     */
    public TrackingStatus trackingStatus() {
        return trackingStatus;
    }

    /**
     * A transaction is the purchase of a shipping label from a shipping provider for a specific service.
     * You can print purchased labels and used them to ship a parcel with a carrier, such as USPS or FedEx.
     */
    public Transactions transactions() {
        return transactions;
    }

    /**
     * A user parcel template represents a package used for shipping that has preset dimensions and
     * attributes defined
     * by you. They are useful for capturing attributes of parcel-types you frequently use for shipping,
     * allowing
     * them to be defined once and then used for many shipments. These parcel templates can also be used
     * for live rates.
     * 
     * <p>User parcel templates can also be created using a carrier parcel template, where the dimensions will
     * be copied from
     * the carrier presets, but the weight can be configured by you.
     */
    public UserParcelTemplates userParcelTemplates() {
        return userParcelTemplates;
    }

    /**
     * Shippo Accounts are used by Shippo Platform Accounts to create and manage Managed Shippo Accounts.
     * Managed Shippo Accounts are headless accounts that represent your customers. They are opaque to your
     * end customers, meaning customers do not need to create their own Shippo login or have a billing
     * relationship with Shippo.
     * They can be used by marketplaces, e-commerce platforms, and third-party logistics providers who want
     * to offer, seamless, built-in shipping functionality to their customers. See our
     * [guide](https://docs.goshippo.com/docs/platformaccounts/platform_accounts/) for more details.
     */
    public ShippoAccounts shippoAccounts() {
        return shippoAccounts;
    }

    /**
     * Webhooks are a way for Shippo to notify your application when a specific event occurs. For example,
     * when a label is purchased or when a shipment tracking status has changed. You can use webhooks to
     * trigger actions in your application, such as sending an email or updating a database.
     */
    public Webhooks webhooks() {
        return webhooks;
    }

    /**
     * The Builder class allows the configuration of a new instance of the SDK.
     */
    public static class Builder {

        private final SDKConfiguration sdkConfiguration = new SDKConfiguration();
        private String serverUrl;
        private String server;
        

        private Builder() {
        }

        /**
         * Allows the default HTTP client to be overridden with a custom implementation.
         *
         * @param client The HTTP client to use for all requests.
         * @return The builder instance.
         */
        public Builder client(HTTPClient client) {
            this.sdkConfiguration.setClient(client);
            return this;
        }
        /**
         * Configures the SDK security to use the provided secret.
         *
         * @param apiKeyHeader The secret to use for all requests.
         * @return The builder instance.
         */
        public Builder apiKeyHeader(String apiKeyHeader) {
            this.sdkConfiguration.setSecuritySource(SecuritySource.of(com.goshippo.shippo_sdk.models.components.Security.builder()
              .apiKeyHeader(apiKeyHeader)
              .build()));
            return this;
        }

        /**
         * Configures the SDK to use a custom security source.
         *
         * @param securitySource The security source to use for all requests.
         * @return The builder instance.
         */
        public Builder securitySource(SecuritySource securitySource) {
            Utils.checkNotNull(securitySource, "securitySource");
            this.sdkConfiguration.setSecuritySource(securitySource);
            return this;
        }
        
        /**
         * Overrides the default server URL.
         *
         * @param serverUrl The server URL to use for all requests.
         * @return The builder instance.
         */
        public Builder serverURL(String serverUrl) {
            this.serverUrl = serverUrl;
            return this;
        }

        /**
         * Overrides the default server URL with a templated URL populated with the provided parameters.
         *
         * @param serverUrl The server URL to use for all requests.
         * @param params The parameters to use when templating the URL.
         * @return The builder instance.
         */
        public Builder serverURL(String serverUrl, Map<String, String> params) {
            this.serverUrl = Utils.templateUrl(serverUrl, params);
            return this;
        }
        
        /**
         * Overrides the default server by index.
         *
         * @param serverIdx The server to use for all requests.
         * @return The builder instance.
         */
        public Builder serverIndex(int serverIdx) {
            this.sdkConfiguration.setServerIdx(serverIdx);
            this.serverUrl= SERVERS[serverIdx];
            return this;
        }
        
        /**
         * Overrides the default configuration for retries
         *
         * @param retryConfig The retry configuration to use for all requests.
         * @return The builder instance.
         */
        public Builder retryConfig(RetryConfig retryConfig) {
            this.sdkConfiguration.setRetryConfig(Optional.of(retryConfig));
            return this;
        }

        /**
         * Enables debug logging for HTTP requests and responses, including JSON body content.
         * <p>
         * Convenience method that calls {@link HTTPClient#enableDebugLogging(boolean)}.
         * {@link SpeakeasyHTTPClient} honors this setting. If you are using a custom HTTP client,
         * it is up to the custom client to honor this setting.
         * </p>
         *
         * @param enabled Whether to enable debug logging.
         * @return The builder instance.
         */
        public Builder enableHTTPDebugLogging(boolean enabled) {
            this.sdkConfiguration.client().enableDebugLogging(enabled);
            return this;
        }


        /**
         * Allows setting the shippoApiVersion parameter for all supported operations.
         *
         * @param shippoApiVersion The value to set.
         * @return The builder instance.
         */
        public Builder shippoApiVersion(String shippoApiVersion) {
            this.sdkConfiguration.globals.putParam("header", "SHIPPO-API-VERSION", shippoApiVersion);
            return this;
        }
        // Visible for testing, may be accessed via reflection in tests
        Builder _hooks(com.goshippo.shippo_sdk.utils.Hooks hooks) {
            sdkConfiguration.setHooks(hooks);  
            return this;  
        }
        
        // Visible for testing, may be accessed via reflection in tests
        Builder _hooks(Consumer<? super com.goshippo.shippo_sdk.utils.Hooks> consumer) {
            consumer.accept(sdkConfiguration.hooks());
            return this;    
        }

        /**
         * Builds a new instance of the SDK.
         *
         * @return The SDK instance.
         */
        public Shippo build() {
            if (serverUrl == null || serverUrl.isBlank()) {
                serverUrl = SERVERS[0];
                sdkConfiguration.setServerIdx(0);
            }
            sdkConfiguration.setServerUrl(serverUrl);
            return new Shippo(sdkConfiguration);
        }
    }

    /**
     * Get a new instance of the SDK builder to configure a new instance of the SDK.
     *
     * @return The SDK builder instance.
     */
    public static Builder builder() {
        return new Builder();
    }

    private Shippo(SDKConfiguration sdkConfiguration) {
        sdkConfiguration.initialize();
        this.addresses = new Addresses(sdkConfiguration);
        this.batches = new Batches(sdkConfiguration);
        this.carrierAccounts = new CarrierAccounts(sdkConfiguration);
        this.customsDeclarations = new CustomsDeclarations(sdkConfiguration);
        this.customsItems = new CustomsItems(sdkConfiguration);
        this.ratesAtCheckout = new RatesAtCheckout(sdkConfiguration);
        this.manifests = new Manifests(sdkConfiguration);
        this.orders = new Orders(sdkConfiguration);
        this.carrierParcelTemplates = new CarrierParcelTemplates(sdkConfiguration);
        this.parcels = new Parcels(sdkConfiguration);
        this.pickups = new Pickups(sdkConfiguration);
        this.rates = new Rates(sdkConfiguration);
        this.refunds = new Refunds(sdkConfiguration);
        this.serviceGroups = new ServiceGroups(sdkConfiguration);
        this.shipments = new Shipments(sdkConfiguration);
        this.trackingStatus = new TrackingStatus(sdkConfiguration);
        this.transactions = new Transactions(sdkConfiguration);
        this.userParcelTemplates = new UserParcelTemplates(sdkConfiguration);
        this.shippoAccounts = new ShippoAccounts(sdkConfiguration);
        this.webhooks = new Webhooks(sdkConfiguration);
        SdkInitData data = sdkConfiguration.hooks().sdkInit(
                new SdkInitData(
                        sdkConfiguration.resolvedServerUrl(), 
                        sdkConfiguration.client()));
        sdkConfiguration.setServerUrl(data.baseUrl());
        sdkConfiguration.setClient(data.client());
    }
}
