/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.goshippo.shippo_sdk;

import static com.goshippo.shippo_sdk.operations.Operations.RequestOperation;

import com.goshippo.shippo_sdk.models.operations.GetRateRequest;
import com.goshippo.shippo_sdk.models.operations.GetRateRequestBuilder;
import com.goshippo.shippo_sdk.models.operations.GetRateResponse;
import com.goshippo.shippo_sdk.models.operations.ListShipmentRatesByCurrencyCodeRequest;
import com.goshippo.shippo_sdk.models.operations.ListShipmentRatesByCurrencyCodeRequestBuilder;
import com.goshippo.shippo_sdk.models.operations.ListShipmentRatesByCurrencyCodeResponse;
import com.goshippo.shippo_sdk.models.operations.ListShipmentRatesRequest;
import com.goshippo.shippo_sdk.models.operations.ListShipmentRatesRequestBuilder;
import com.goshippo.shippo_sdk.models.operations.ListShipmentRatesResponse;
import com.goshippo.shippo_sdk.operations.GetRate;
import com.goshippo.shippo_sdk.operations.ListShipmentRates;
import com.goshippo.shippo_sdk.operations.ListShipmentRatesByCurrencyCode;
import com.goshippo.shippo_sdk.utils.Headers;
import java.lang.Long;
import java.lang.String;
import java.util.Optional;

/**
 * A rate is the cost to ship a parcel from a carrier. The rate object details the service level
 * including the cost and transit time.
 */
public class Rates {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;

    Rates(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
    }

    /**
     * Retrieve a rate
     * 
     * <p>Returns an existing rate using a rate object ID. Rates older than 390 days are not returned.
     * 
     * @return The call builder
     */
    public GetRateRequestBuilder get() {
        return new GetRateRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve a rate
     * 
     * <p>Returns an existing rate using a rate object ID. Rates older than 390 days are not returned.
     * 
     * @param rateId Object ID of the rate
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetRateResponse get(String rateId) {
        GetRateRequest request =
            GetRateRequest
                .builder()
                .rateId(rateId)
                .build();
        RequestOperation<GetRateRequest, GetRateResponse> operation
              = new GetRate.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Retrieve shipment rates
     * 
     * <p>Returns a paginated list of rates associated with a shipment. Rates for shipments older than 390
     * days are not returned.
     * 
     * @return The call builder
     */
    public ListShipmentRatesRequestBuilder listShipmentRates() {
        return new ListShipmentRatesRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve shipment rates
     * 
     * <p>Returns a paginated list of rates associated with a shipment. Rates for shipments older than 390
     * days are not returned.
     * 
     * @param shipmentId Object ID of the shipment to update
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListShipmentRatesResponse listShipmentRates(String shipmentId) {
        return listShipmentRates(shipmentId, Optional.empty(), Optional.empty());
    }

    /**
     * Retrieve shipment rates
     * 
     * <p>Returns a paginated list of rates associated with a shipment. Rates for shipments older than 390
     * days are not returned.
     * 
     * @param shipmentId Object ID of the shipment to update
     * @param page The page number you want to select
     * @param results The number of results to return per page (max 100)
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListShipmentRatesResponse listShipmentRates(
            String shipmentId, Optional<Long> page,
            Optional<Long> results) {
        ListShipmentRatesRequest request =
            ListShipmentRatesRequest
                .builder()
                .shipmentId(shipmentId)
                .page(page)
                .results(results)
                .build();
        RequestOperation<ListShipmentRatesRequest, ListShipmentRatesResponse> operation
              = new ListShipmentRates.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Retrieve shipment rates in currency
     * 
     * <p>Returns all available shipping rates for a shipment object.
     * 
     * <p>When you create a new valid shipment object, Shippo automatically calculates all available rates.
     * Depending on your shipment data, there may be none, one or multiple rates.
     * 
     * <p>By default, the calculated rates will return the price in two currencies under the `amount` and
     * `amount_local` keys, respectively. The `amount` key will contain the price of a rate expressed in
     * the currency that is used in the country from where the parcel originates, and the `amount_local`
     * key will contain the price expressed in the currency that is used in the country the parcel is
     * shipped to. You can request rates with prices expressed in a different currency by adding the
     * currency code to the end of the resource URL.
     * 
     * <p>The full list of supported currencies along with their codes can be viewed on [open exchange
     * rates](http://openexchangerates.org/api/currencies.json).
     * 
     * <p>Note: re-requesting the rates with a different currency code will re-queue the shipment (i.e. set
     * the Shipment's `status` to `QUEUED`) and the converted currency rates will only be available when
     * the Shipment's `status` is set to `SUCCESS`.
     * 
     * <p>Rates for shipments older than 390 days are not returned.
     * 
     * @return The call builder
     */
    public ListShipmentRatesByCurrencyCodeRequestBuilder listShipmentRatesByCurrencyCode() {
        return new ListShipmentRatesByCurrencyCodeRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve shipment rates in currency
     * 
     * <p>Returns all available shipping rates for a shipment object.
     * 
     * <p>When you create a new valid shipment object, Shippo automatically calculates all available rates.
     * Depending on your shipment data, there may be none, one or multiple rates.
     * 
     * <p>By default, the calculated rates will return the price in two currencies under the `amount` and
     * `amount_local` keys, respectively. The `amount` key will contain the price of a rate expressed in
     * the currency that is used in the country from where the parcel originates, and the `amount_local`
     * key will contain the price expressed in the currency that is used in the country the parcel is
     * shipped to. You can request rates with prices expressed in a different currency by adding the
     * currency code to the end of the resource URL.
     * 
     * <p>The full list of supported currencies along with their codes can be viewed on [open exchange
     * rates](http://openexchangerates.org/api/currencies.json).
     * 
     * <p>Note: re-requesting the rates with a different currency code will re-queue the shipment (i.e. set
     * the Shipment's `status` to `QUEUED`) and the converted currency rates will only be available when
     * the Shipment's `status` is set to `SUCCESS`.
     * 
     * <p>Rates for shipments older than 390 days are not returned.
     * 
     * @param shipmentId Object ID of the shipment to update
     * @param currencyCode ISO currency code for the rates
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListShipmentRatesByCurrencyCodeResponse listShipmentRatesByCurrencyCode(String shipmentId, String currencyCode) {
        return listShipmentRatesByCurrencyCode(shipmentId, currencyCode, Optional.empty(),
            Optional.empty());
    }

    /**
     * Retrieve shipment rates in currency
     * 
     * <p>Returns all available shipping rates for a shipment object.
     * 
     * <p>When you create a new valid shipment object, Shippo automatically calculates all available rates.
     * Depending on your shipment data, there may be none, one or multiple rates.
     * 
     * <p>By default, the calculated rates will return the price in two currencies under the `amount` and
     * `amount_local` keys, respectively. The `amount` key will contain the price of a rate expressed in
     * the currency that is used in the country from where the parcel originates, and the `amount_local`
     * key will contain the price expressed in the currency that is used in the country the parcel is
     * shipped to. You can request rates with prices expressed in a different currency by adding the
     * currency code to the end of the resource URL.
     * 
     * <p>The full list of supported currencies along with their codes can be viewed on [open exchange
     * rates](http://openexchangerates.org/api/currencies.json).
     * 
     * <p>Note: re-requesting the rates with a different currency code will re-queue the shipment (i.e. set
     * the Shipment's `status` to `QUEUED`) and the converted currency rates will only be available when
     * the Shipment's `status` is set to `SUCCESS`.
     * 
     * <p>Rates for shipments older than 390 days are not returned.
     * 
     * @param shipmentId Object ID of the shipment to update
     * @param currencyCode ISO currency code for the rates
     * @param page The page number you want to select
     * @param results The number of results to return per page (max 100)
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListShipmentRatesByCurrencyCodeResponse listShipmentRatesByCurrencyCode(
            String shipmentId, String currencyCode,
            Optional<Long> page, Optional<Long> results) {
        ListShipmentRatesByCurrencyCodeRequest request =
            ListShipmentRatesByCurrencyCodeRequest
                .builder()
                .shipmentId(shipmentId)
                .currencyCode(currencyCode)
                .page(page)
                .results(results)
                .build();
        RequestOperation<ListShipmentRatesByCurrencyCodeRequest, ListShipmentRatesByCurrencyCodeResponse> operation
              = new ListShipmentRatesByCurrencyCode.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
